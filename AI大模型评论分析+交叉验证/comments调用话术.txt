我需要你帮我完成一个“娃衣评论AI大模型分析”项目，最终产出 Python 代码和一份分析报告。

我收集了 150 条来自小红书/微博的真实评论，每条有 id、comment、likes（点赞数）。现在需要用大模型（即你）对这些评论做深度分析，并与我的问卷结论交叉验证。

【数据样例】
id,comment,likes
1,考研压力大时给娃娃换装拍照，特别治愈，瞬间舒缓心情,45
2,深夜复习看着穿新衣的娃娃，孤单感少了很多，心里格外温暖,32
3,备考焦虑全靠搭配娃衣解压，暂时抛开学习烦恼轻松不少,28
...（共150条，我会提供完整CSV）

【分析要求】
请生成一份完整的 Python 脚本（单文件，可直接运行），实现以下所有功能。要求代码健壮、有注释、能处理中文路径和编码问题。

1. **数据加载**：自动检测 CSV 编码，读取 id, comment, likes。

2.   - 每条评论调用一次，输出 JSON 格式，包含：
     - sentiment: 正面/负面/中性
     - intensity: 情感强度 (0~1)
     - themes: 数组，可选 [审美,情感补偿,社交,收藏,价格,身份认同]
     - emotions: 数组，可选 [开心,治愈,解压,惊喜,失望,焦虑,孤独,温暖,陪伴]
   - 处理 API 限流（每次间隔 0.3 秒），自动重试一次。
   - 进度保存：每分析 10 条保存一次中间结果到 JSON，防止中断丢失。

3. **加权统计分析**：
   - 按点赞数加权计算情感分布（正面/负面/中性占比）。
   - 加权计算各主题提及率（%）。
   - 加权计算各情绪词频率。

4. **情感强度与点赞数相关性**：计算 Pearson 相关系数，并解释。

5. **主题共现网络**：绘制主题共现网络图（networkx + matplotlib），保存为 PNG。

6. **消费者自动聚类**：
   - 基于情感强度、点赞数、主题偏好（多热编码）做 KMeans 聚类（k=4）。
   - 输出每个聚类的平均情感强度、平均点赞数、前3主题。
   - 与论文中的四类（情感驱动型、身份认同型、深度社群型、浅尝辄止型）做文字对比。

7. **AI 生成营销文案**：
   - 选取点赞数最高的 5 条正面评论，生成 3 条小红书风格种草文案（治愈陪伴型、审美种草型、冲动安利型）。

8. **加权词云**：
   - 以点赞数为权重，生成词云图
9. **交叉验证总结**：
   - 将 AI 发现（主题提及率、情感强度等）与我们做得最终结论（完整分析链路.ipynb）做对比，输出一致性判断。

10. **输出保存**：
    -本次生成的所有内容都放在'comments大模型分析'目录下
    - 所有结果（分析后的 DataFrame）保存为 CSV。
    - 所有图表保存到 `comments大模型分析\images_comments` 目录。
    - 在控制台打印完整的分析结果和验证结论。

【额外要求】
- 不要使用全局 checkpoint 路径，所有文件读写都基于当前脚本所在目录。
- 如果生成词云时遇到字体问题，自动回退到 matplotlib 能用的中文字体。

【数据】
我会在同级目录下放一个 `comments.csv` 文件（你只需要读取它，不需要嵌入数据）